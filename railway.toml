[build]
builder = "NIXPACKS"
buildCommand = "node scripts/railway.mjs gateway"

[deploy]
startCommand = "npm run start -w @robinexis/voice-gateway"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
