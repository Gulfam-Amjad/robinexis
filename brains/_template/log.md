# Log — {{Brain Name}}

Append-only timeline. One line per event, newest at the bottom. Format: `## [YYYY-MM-DD] <type> | <title>`.

Grep the recent history with: `grep "^## \[" log.md | tail -10`

## [{{date}}] scaffold | Brain created from brains/_template
